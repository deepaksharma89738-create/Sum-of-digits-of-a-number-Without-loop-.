# Sum-of-digits-of-a-number-Without-loop-.
Sum of digits without using a loop can be done using recursion. The function separates the last digit of the number using the modulus operator (%) and adds it to the result of the remaining number obtained by dividing the number by 10. This process continues until the number becomes 0, which acts as the base case and stops the recursion.
